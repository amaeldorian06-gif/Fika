# ---- Base de données (Prisma / PostgreSQL) ----
DATABASE_URL="postgresql://postgres:[Kid1joyland'@]@db.kwroxvifjnunoihoadbw.supabase.co:5432/postgres"

# ---- WhatsApp — numéro de production au format E.164 (config unique) ----
# Numéro validé par le fondateur (2026-01-08) : +237 671 164 936
# Port Vite : VITE_WA_PHONE — en Next.js : NEXT_PUBLIC_WA_PHONE
VITE_WA_PHONE="+237671164936"
NEXT_PUBLIC_WA_PHONE="+237671164936"

# ---- Application ----
APP_URL="https://fika.cm"              # TODO_PROD : domaine à confirmer

# ---- Admin fondateur (utilisé par prisma/seed.ts) ----
# Identifiant du compte seed — VALIDÉ par le fondateur (2026-01-08) :
ADMIN_EMAIL="amaeldorian06@gmail.com"
# Sécurité : préférer ADMIN_PASSWORD_HASH en production :
#   node -e "console.log(require('bcryptjs').hashSync(process.argv[1],10))" 'Kid1joyland'@'
# ADMIN_PASSWORD (clair) est toléré en local uniquement — le seed le hache
# en bcrypt à l'exécution, ne jamais le committer dans du code ou un README.
# ADMIN_PASSWORD="Kid1joyland'@"
# ADMIN_PASSWORD_HASH="Kid1joyland'@"

# ---- Sessions back-office (P06) — OBLIGATOIRE en production ----
# Secret de signature du cookie de session (32 caractères minimum) :
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
AUTH_SECRET="Kid1joyland'@"
